; /etc/asterisk/http.conf — Asterisk's built-in HTTP server, required for ARI.
; Bound to localhost because the Node app runs on the same box.

[general]
enabled = yes
bindaddr = 127.0.0.1
bindport = 8088
